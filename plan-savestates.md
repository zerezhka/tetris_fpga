# План: save/load состояния HT943

Статус: **РЕАЛИЗОВАНО, собрано и залито** (2026-07-07, rbf
HT943_20260707_0209). Core-сериализация (150-байтовый flat view: ram+регистры),
FSM-инжект через reset-ветку, префетч-прайминг с восстановленного PC,
HT943-обвязка (F4 .sav + OSD-gated автосейв), tb+regression — всё сделано.
Симуляция: roundtrip бит-в-бит (N=1/500/2000/3333, вкл. SI) PASS, полная
регрессия зелёная. Железо: rbf 0 errors, залит; load-путь подтверждён (.sav
стримится через ioctl index 4 и меняет состояние). Save-путь (автосейв при
открытии OSD) — подтверждается пользователем на устройстве (OSD headless не
открыть). Состояние крошечное (~146 байт), но путь загрузки инжектит в
cycle-exact CPU — сделано аккуратно, при ss_apply=0 поведение байт-в-байт
прежнее (регрессия это гарантирует).

## Что входит в стейт (архитектурное, ht943_core.sv:225–249)

- `ram[0:255]` — 256 нибблов = **128 байт** (основное).
- Регистры (~18 байт): `r_pc`[12], `r_acc`[4], `r_wr[0:4]`[20], `r_stack`[13],
  `r_ei/r_cf/r_tf/r_ef/r_halt/r_timerf`[6], `r_tc`[8], `r_timer_cnt`[16],
  `r_pa`[4], `r_pp_prev/r_pm_prev/r_ps_prev`[12], звук:
  `r_snd_on/r_snd_repeat/r_snd_channel/r_snd_note_ctr/r_snd_clk_cnt/
  r_snd_tick/r_snd_tick_note/r_snd_tick_fx`.

НЕ входит (восстанавливается загрузкой картриджа): `r_timer_div`,
`r_pp/pm/ps_wakeup`, `r_sound_freq_div`, ROM (`rom16_*`), `sound_rom`,
`speed_div`, `sound_fx`. Пайплайн префетча (`r_rom16_q*`, `r_byte0`,
`r_wrap_*`) — не сохраняем: он пере-праймится из `r_pc` при снятии reset.

## Механизм (нет SS-фреймворка → через ioctl save-канал)

`sys/hps_io.sv` в этом репо **не имеет** ss-портов (слоты 1-4 через DDR), но
имеет `ioctl_upload`/`ioctl_upload_req`/`ioctl_upload_index`/`ioctl_din`/
`ioctl_rd`. Значит save/load — через тот же ioctl-канал (как cartridge-save
у NES-подобных ядер):

- **SAVE**: HPS дёргает `ioctl_upload_req`; ядро отдаёт ~146 байт стейта по
  `ioctl_din`/`ioctl_rd` по мере роста `ioctl_addr`; HPS пишет в `.sav`.
- **LOAD**: HPS стримит `.sav` через `ioctl_download` c save-индексом; ядро
  десериализует в стейт.

### Выбрано: A. Авто-`.sav` «продолжить» (пользователь, 2026-07-06)

Механизм подтверждён по офиц. доке hps_io + референс-ядру C128:
- **SAVE (автосейв)**: ядро держит `ioctl_upload_req = autosave & OSD_STATUS
  & dirty`. HPS **читает `ioctl_upload_req` только когда OSD открыт** →
  при открытии меню вычитывает ~146 байт стейта через `ioctl_din`/
  `ioctl_upload` и пишет `.sav`. `ioctl_upload_index` = наш save-индекс.
- **LOAD (автозагрузка)**: HPS авто-стримит `.sav` через `ioctl_download`
  при загрузке игры; ядро ловит по `ioctl_index` (save-индекс, НЕ 1/2/3),
  десериализует в стейт.
- **CONF_STR**: объявить save-файл (расширение `.sav`, свой индекс) +
  тумблер `Autosave On/Off`. Нужен сигнал `OSD_STATUS` из hps_io (проверить,
  выведен ли; C128 его использует).
- **Порты hps_io уже есть** (`ioctl_upload/_req/_index/din/rd`) — сейчас в
  HT943.sv не подключены; подключить.

Вариант B (ручные слоты 1-4 через SS-фреймворк + DDR) — отложен.

## Изменения ядра (ht943_core.sv) — делаем осторожно

1. **Readout**: вывести НЕэкспонированные `r_*` наружу (часть уже трейс-
   выходы) — компактный `ss_state_out` или отдельные порты. RAM уже читается
   через `dbg_ram_addr`/`dbg_ram_data`.
2. **Инжект**: новый вход `ss_load` + `ss_state_in`/адрес; при `ss_load`
   (держится как спец-reset после загрузки картриджа) перезаписать `ram[]` и
   все `r_*` из потока, затем нормальный старт. Не трогать fetch-пайплайн
   (он пере-праймится сам).
3. Порядок при загрузке: pak → reset+инициализация ROM/config → затем `.sav`
   инжектит стейт → снятие reset → игра продолжается.

## Обвязка (HT943.sv) + отдельный модуль

- `ht943_savestate.sv`: FSM сериализации/десериализации (walk 128 RAM
  нибблов + пакует регистры в байты) ↔ ioctl save-канал.
- CONF_STR: объявить поддержку сейва (индекс save-файла); при варианте B —
  ещё пункты меню Save/Load state.

## Тесты

- Sim: прогнать N инструкций, сериализовать → сбросить → десериализовать →
  убедиться, что стейт бит-в-бит совпал и исполнение продолжается идентично
  (golden-trace до и после совпадают).
- `sim/regression.py` all-PASS (CPU-правки не должны ничего сломать).

## Сборка/деплой

- `quartus_sh` пересборка; регрессия; деплой rbf; проверка на железе:
  поиграл → сохранил → сбросил ядро → загрузил → продолжил с того же места.

## НЕ в этом инкременте

- Слоты-через-DDR (вариант B), rewind. Только если пользователь захочет
  полноценные quick-save слоты.
