# План: загружаемые «device packs» (лица + профили с линукса)

Статус: **сделано 2026-07-05** — шаги 1–5, все 6 устройств (4 старых +
E23 96in1 + GA888) проверены на железе скриншотами; M10K упал до 16%
от чипа. Полировка графики продолжена 2026-07-06 — см. раздел
«Полировка графики (2026-07-06)» ниже. Контекст на момент плана: сейчас все 4 лица запечены в битстрим (4 комплекта
таблиц в M10K, 257/553 блоков), а профиль устройства (клоки, маски, звук,
кнопки) — localparam-массивы из `rtl/ht943_profiles.svh`. Добавление каждого
нового устройства = пересборка ПЛИС (+~90 M10K за пару лиц). Это тупик и по
памяти, и по UX.

## Идея

Устройство целиком описывается **файлами на SD**, ядро становится
универсальным HT943-интерпретатором:

```
games/HT943/E88_8in1.bin   — ROM (как сейчас, F1)
games/HT943/E88_8in1.sro   — sound ROM (как сейчас, F2)
games/HT943/E88_8in1.pak   — device pack (НОВОЕ, F3): лицо + профиль
```

`.mgl` лаунчер грузит все три одним кликом — UX не меняется. Понятие
«ROM profile» из RTL исчезает; CRC-автодетект остаётся только как фолбэк
для голого .bin без пака.

## Формат `.pak` (генерирует tools/gen_device_pack.py из .brick + .svg)

Little-endian, фиксированные секции, магия+версия в заголовке:

| секция | размер | содержимое |
|---|---|---|
| header | 32 B | magic "HTPK", версия, CRC32 ROM-а (для сверки), офсеты секций |
| config | ~256 B | clk_div, timer_div, wakeup/pullup маски, jmap PP/PM/PS, reset_jmap, sound spd/fx таблицы — всё, что сейчас в svh |
| frame | 8 B | рамка стакана x0,y0,x1,y1 |
| segtab | 512×2 B | {byte,bit} на сегмент |
| geotab | 512×8 B | 54-битное гео-слово (bbox+метрики кирпича) |
| pixmap | 33600×2 B | коарс-карта 120×280, 10 бит/слово |

Итого ~72 КБ — заливается через ioctl мгновенно.

## Изменения в RTL

1. `HT943.sv`: `"F3,PAK,Load Device;"`, декод `ioctl_index[5:0]==3`;
   FSM-распаковщик: считает байты, собирает 16/64-битные слова, пишет в
   таблицы. Пока идёт download — ядро в ресете (путь `cfg_active` уже есть).
2. `rtl/ht943_lcd.sv`: таблицы из 4×ROM → 1×RAM с портом записи
   (simple dual-port M10K). ~46 блоков вместо ~200. Инициализация
   `$readmemh` контентом E88 — фолбэк-лицо при голом .bin.
3. `rtl/ht943_core.sv` / обвязка: конфиг-регистры грузятся из секции config
   (сейчас — мультиплексор по profile_sel из localparam; станет
   write-once регистрами от распаковщика). CRC-автодетект переключает
   только фолбэк-набор.
4. Осторожно с инференсом RAM у Quartus 17: один чистый write-порт,
   отдельный `always_ff` на чтение (грабли rom16 задокументированы в
   памяти проекта / git history).

## Тесты

- `sim/test_device_pack.py`: pack→unpack roundtrip против svh/hex ассетов;
- Verilator-тест распаковщика: скормить .pak побайтово (как ioctl),
  сверить содержимое таблиц с эталонными hex;
- lcd smoke: рендер после загрузки пака == рендер от запечённых таблиц;
- регрессия целиком.

## Выигрыш

- M10K: ~257 → ~100 (ядро+одно лицо), потолок исчезает;
- новые устройства (E23 96-in-1, GA888, будущие дампы) = новые файлы,
  без пересборки; E23/GA888 добавляются сразу после этой работы как
  чистая генерация паков;
- один битстрим навсегда (почти).

## Порядок работ

1. Генератор паков + roundtrip-тест (чистый Python, полдня).
2. LCD-таблицы ROM→RAM + распаковщик + Verilator-тест (основная часть).
3. Конфиг-секция → регистры, выпил PROFILE_* мультиплексоров.
4. mgl-и с тремя файлами, деплой, проверка 4 устройств на железе.
5. Паки для E23 + GA888 (у них уже есть .brick/.svg/.bin в BrickEmuPy),
   проверка на железе — 6 устройств.

## Полировка графики (2026-07-06)

Ghost cells как диагностика вскрыли всю геометрию — по ней прошлись:

**Сделано (только паки, без пересборки ПЛИС):**
- **LCD-персистентность («затухание»)** + два OSD-тумблера: `O1` LCD
  persistence (дефолт On), `O2` Ghost cells (дефолт Off). 4-битный
  аккумулятор на сегмент, FSM обходит 512 индексов в vblank, 5-уровневый
  LUT. Тумблеры на младших битах — Main теряет старший бит статуса при
  восстановлении .CFG. Персистентность-дефолт подтверждена на железе;
  сами тумблеры проверены пользователем в OSD (headless CFG-инъекция не
  работает через load_core — только OSD/boot-init восстанавливают .CFG).
- **SpaceIntruder projectile** — выстрел (26 тонких 2px-сегментов в 2
  колонках) рендерился разного размера от округлений коарс-карты; теперь
  единый 2×11 сплошной.
- **E23 кирпичи** — были 0 «кирпичей» (детектор ищет полый frame+gap, а
  E23 рисует сплошные), падали в коарс-заливку → ширина гуляла 19↔20.
  Реальное железо (фото пользователя) подтверждает: **кирпичи E23
  сплошные, не полые** как у E88. Теперь равномерная сплошная сетка.
- Механизм: `tools/extract_segments_mask.py`, «solid-grid» классификатор
  — кластеры сплошных (cov≥0.9) не-кирпичных сегментов в двух полосах:
  тонкие (min≤2: точки/выстрелы) и крупные (min≥10: ячейки поля) →
  единый процедурный сплошной прямоугольник. Средняя полоса 3..9px
  (пришельцы SI, мелкие иконки) не трогается — узнаваемая форма. Хирургично:
  меняется только целевое лицо, остальные байт-в-байт те же.

**Наблюдения про реализм LCD (от пользователя, по фото/видео живых
устройств) — SVG BrickEmuPy = самый «правильный» референс:**
- У реальной матрицы **нет «плотности» в лоб**: незажжённые сегменты
  практически не видны, фоновой сетки нет. Гостинг (слабый узор всех
  сегментов) проступает **только под углом** к экрану.
- Наши режимы ложатся ровно: **Ghost OFF ≈ вид в лоб** (чисто),
  **Ghost ON ≈ вид под углом** и одновременно вид самого BrickEmuPy
  (он рисует теневую копию каждого сегмента `opacity=0.1` + motion blur —
  т.е. наши ghost≈% и persistence мирроринг эталонного эмулятора).
- Решение: интенсивность ghost снижена 10%→**5%** (тоньше, ближе к
  «в лоб почти невидимо»). Значение субъективно и headless не
  проверяется. **Правка в `rtl/ht943_lcd.sv` (GH_* делитель /20) уже в
  коде, но ДРЕМЛЕТ — вступит в силу при следующей пересборке ПЛИС**
  (ради одного этого битстрим не гоняли).

**Хвосты графики (все проверены 2026-07-06):**
- **Рамка стакана E23** — ЗАКРЫТО. Крупные solid-grid кластеры теперь
  сидят well-рамку → E23 frame `[0,97,231,840]`. Только пак; уже в v2-паках
  на SD (byte-identical свежей сборке).
- **NEXT-ячейки E88** — НЕ ДЕФЕКТ. Проверено дампом кластеров: все 8
  NEXT-ячеек (segs `186_*`/`188_*`) попадают в ОДИН size-кластер и
  снапятся к единому `15×24` (проход uniformize из `d891469`). Wobble,
  что видел пользователь, был на СТАРЫХ v1-паках; в текущих v2 ячейки
  одинаковы. (Коарс-карта владения по-прежнему рваная по своей природе,
  но финальный рендер берёт униформленный bbox, а не карту владения.)
- **Форма «укрытий» SpaceIntruder** — ЗАКРЫТО fine-mask'ом. Укрытия —
  non-brick сегменты (294 из 320), попадают в `_ink.hex` и рендерятся
  точной 1bpp SVG-формой. На fine-mask-превью нижний ряд бункеров ровный.
  Оживёт с fine-mask-битстримом (dormant, см. `plan-fine-mask-renderer.md`).

**Решение по UX загрузки (2026-07-06):** оставляем **3 файла + .mgl**
(bin+sro+pak, один клик из _Console). Комбинированный единый файл
рассматривали, но: (а) FPGA не может сам дозагрузить соседние файлы —
это делает только ARM-часть MiSTer; (б) убрать ручные F2/F3-загрузчики
нельзя — mgl сверяет свои индексы с F-пунктами CONF_STR и без них
ломается (проверено на железе: устройства падали в пустой E88-фолбэк).
Единственный способ «выбрал один — загрузилось всё» — комбинированный
файл (ROM+звук+лицо в одном, +RTL-распаковка ROM/звука, пересборка) —
отложено по решению пользователя.

## Очередь после паков (по приоритету)

1. ~~**Звук**: послушать~~ — сделано 2026-07-06, звук на месте (по
   словам пользователя).
2. **FC-флаг** в CONF_STR: запоминать последние файлы — после паков
   особенно приятно (3 файла руками — боль без mgl). Актуально: UX
   загрузки решено оставить 3-файловым (см. выше), FC тут особенно кстати.
3. NEXT-ячейки E88 — перенесено в «Открытые хвосты графики» выше.
4. Savestates (RAM 256×4 + регистры — крошечный стейт, механика MiSTer
   стандартная).
5. Homebrew «canonical tetris» ROM + Pico/Zig handheld — см.
   `brickgame-fpga-roadmap.md`, раздел 7.
